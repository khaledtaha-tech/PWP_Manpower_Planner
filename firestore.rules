rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never accesses Firestore directly. All reads and writes go
    // through the authenticated Node server, which uses Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
